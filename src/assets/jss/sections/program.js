import { makeStyles } from "@material-ui/styles";
import { flex, verticalCenter } from "../cbb-material-ui";

const useStyles = makeStyles({
  container: {
    height: 600,
  },
  image: {
    width: "100%",
  },
  flex,
  verticalCenter,
});

export default useStyles;
